from tela_pessoa import TelaPessoa

class ControladorPessoaJuridica:
    def __init__(self, tela: TelaPessoa):
        if isinstance(tela, TelaPessoa):
            self.__tela = tela
        self.__pessoas = []


    def lista_pessoas(self):
        self.__tela_pessoa_.mostra_pessoas({"nome: " pessoa.nome)


    def adiciona_pessoa_juridica(self, nome, str):
        if isinstance(nome, str):
        nome_pessoa = self.__tela_pessoa.seleciona_pessoa()
        pessoa = self.pega_dado_pessoa_por_nome(nome_pessoa)

        if (nome_pessoa is None):
            self.__pessoas.append(pessoa)
            self.lista_pessoas()
        else:
            self.__tela_pessoa_.mostra_mensagem("ATENCAO: Esta pessoa não existe!")


    def exclui_pessoa(self):
       if isinstance(nome):
        nome_pessoa = self.__tela_pessoa.seleciona_pessoa()
        pessoa = self.pega_dado_pessoa_por_nome(nome_pessoa)

        if (pessoa is not None):
            self.__pessoas.remove(pessoa)
            self.lista_pessoas()
        else:
            self.__tela_pessoa_.mostra_mensagem("ATENCAO: Esta pessoa já existe!")


    def altera_pessoa:
    


    def acha_pessoa:



    def lista_pessoa:
